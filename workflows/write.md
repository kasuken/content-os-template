# WRITE workflow

Create a draft from an approved brief. Follow `system/` and the applicable channel rules. Preserve the configured point of view, use supportable specifics, and avoid generic AI phrasing.

Output the draft plus an optional alternative hook and closing line. Save it in the channel's drafting folder.
