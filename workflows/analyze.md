# ANALYZE workflow

Analyze published content using verified performance data and qualitative feedback. Separate facts from interpretations. Capture patterns to reuse, weak points, experiments, and next actions in `feedback/` or `examples/`.
